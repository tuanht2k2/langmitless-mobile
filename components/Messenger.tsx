import React from "react";
import { ScrollView, View } from "react-native";

interface IProps {
  id?: string;
}

function Messenger(props: IProps) {
  return <ScrollView style={{ height: "100%" }}></ScrollView>;
}

export default Messenger;
